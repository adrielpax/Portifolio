# node_modules plano (sem junctions/symlinks).
# O caminho deste projeto é longo demais para o layout padrão do pnpm no
# Windows: o store trunca nomes de pastas e o Turbopack não resolve módulos
# através desses junctions (ex.: @radix-ui/react-use-controllable-state).
node-linker=hoisted
